# Rules for `.cursor/hooks/**/*.py`

# Rules for `.cursor/hooks/**/*.py`

## Conventions

- **lowercase constant naming**: Name constants using lowercase style.
  *Example context from `.cursor/hooks/klaussy_dependency_guard.py` (lines 82-86):*
  ```python
      """Positional package names among a verb's arguments (flags/paths excluded)."""
      pkgs: list[str] = []
      skip_next = False
      for tok in args:
          if skip_next:
  ```
